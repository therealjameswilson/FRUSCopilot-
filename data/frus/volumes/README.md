Place FRUS XML volume files in this directory.

This directory is intentionally tracked as regular repository content (not a git submodule)
so GitHub Pages / Actions checkout does not require submodule configuration.
